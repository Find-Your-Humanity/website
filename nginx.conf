server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router SPA
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 기본 보안 헤더 (문제 가능성 있는 항목 제거)
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
}